# PyFEA dependencies.
# Verified working on Python 3.14 (Windows) with the versions in parentheses.
numpy>=1.26          # (tested 2.4.6)   linear algebra / array math
scipy>=1.11          # (tested 1.17.1)  sparse assembly + sparse direct solver
matplotlib>=3.7      # (tested 3.10.9)  force-displacement plot
meshio>=5.3          # (tested 5.3.5)   STL import
pyvista>=0.43        # (tested 0.48.4)  3D mesh + results rendering
pyvistaqt>=0.11      # (tested 0.11.4)  embed PyVista in the Qt window
PyQt5>=5.15          # (tested 5.15.11) desktop GUI
tetgen>=0.6          # (tested 0.8.4)   STL surface -> tetrahedral volume mesh
vtk>=9.3             # (tested 9.6.2)   rendering backend (pulled in by pyvista)
